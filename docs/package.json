{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run api:generate && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages --dotfiles -d .output/public",
    "api:generate": "ts-node --esm ./scripts/generate-api.ts"
  },
  "devDependencies": {
    "@nuxt/content": "^2.8.2",
    "gh-pages": "^6.0.0",
    "nuxt": "^3.7.3",
    "ts-node": "^10.9.1"
  }
}
