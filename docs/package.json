{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "npm run docs:build && nuxt generate && cp pm.config .output/public/",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "clean": "ts-node --esm ./scripts/clean.ts",
    "deploy": "gh-pages --dotfiles -d .output/public",
    "docs:build": "ts-node --esm ./scripts/build-docs.ts"
  },
  "devDependencies": {
    "@nuxt/content": "^2.8.5",
    "@nuxt/devtools": "^0.8.5",
    "@nuxtjs/color-mode": "^3.3.0",
    "@nuxtjs/tailwindcss": "6.8.0",
    "@tailwindcss/typography": "^0.5.10",
    "daisyui": "^3.8.1",
    "gh-pages": "^6.0.0",
    "nuxt": "^3.7.4",
    "nuxt-icon": "^0.5.0",
    "ts-node": "^10.9.1"
  }
}
